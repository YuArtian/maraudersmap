> PDM 是一个 Python 的包管理器和项目构建工具，旨在提供一种简单、高效的方式来管理 Python 项目的依赖关系和构建过程
> https://pdm-project.org/zh-cn/latest/

> [!note] 定位
> PDM 干的活（`pyproject.toml` + 锁文件 + 虚拟环境 + 装包）已被 [[0.05 uv|uv]] 完整覆盖，且 uv 还多管解释器版本。新项目直接用 uv；对照表见 [[0.05 uv]] 第 1 节。

