#iOS #index

# iOS 知识地图

> Library/iOS 总索引：目前聚焦在依赖管理与 Xcode 故障排查

## 文章清单

| 文章 | 一句话定位 |
|---|---|
| [[iOS 依赖管理：SPM 与 CocoaPods]] | 两套竞争的依赖管理器：SPM 由 Xcode 内建自动解析，CocoaPods 靠 `pod install` + workspace |
| [[Xcode SPM 依赖解析失败排查]] | 满屏 `Missing package product` 通常只是某一个包没下全——SPM 解析是「全有或全无」 |

---

## 跨目录关联

- 同一个问题在 JS 生态的形态（lock 文件、精确还原）→ [[Marauder'sMap/Library/Node包管理器/Index|Node 包管理器索引]]、[[1.02 package.json 与依赖管理]]
- 版本范围符的通用约定 → [[语义版本控制 SemVer]]
- 移动端与 WebView 的通信 → [[2.06 移动端适配与 JSBridge]]
- 跨平台的另一条路 → [[Flutter 中好用的库]]

---

## 维护说明

本目录暂不编号。攒够语言（Swift）、UI（SwiftUI/UIKit）、工程（依赖与构建）三类内容后再分组。
