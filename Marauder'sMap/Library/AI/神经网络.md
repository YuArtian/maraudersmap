#AI #ML #deep-learning #neural-network

# 神经网络（Neural Network）

> 它就是一个函数：输入几个数，输出几个数，中间全是乘法和加法

## TL;DR

- **神经网络 = 一个带大量待定参数的复合函数**，整个定义就是 $y = \sigma(W_2 \cdot \text{ReLU}(W_1 x + b_1) + b_2)$
- **神经元** = 加权求和 + 偏置 + 激活函数（最小计算单元）
- **权重**决定各输入的重要性（斜率）；**偏置**是门槛（这个神经元要多强的信号才发声）
- **激活函数**破坏线性，否则叠再多层也等价于一层；最常用 ReLU（负数归零）
- **隐藏层**之所以"隐藏"，是因为它的输出既非你给的输入、也非最终结果，从外部看不见
- **一层 = 一次矩阵乘法**，整层神经元并行算完 → 这是 GPU 派上用场的地方
- **训练**就是找出全部权重和偏置；"7B 参数"就是这样的数有 70 亿个
- **"神经"是历史包袱**，与真实大脑关系很浅，不必从生物学角度理解

---

## 1. 先看它算出一个数

任务：根据"复习小时数"和"睡眠小时数"，预测考试能否通过。
结构：2 个输入 → 2 个隐藏神经元 → 1 个输出。

全部参数如下（先别管哪来的，第 7 节说）：

```text
隐藏神经元 h₁:  权重 (0.5, 0.2)   偏置 −4
隐藏神经元 h₂:  权重 (0.1, 0.5)   偏置 −3
输出神经元  y:  权重 (1.5, 0.8)   偏置 −1
```

输入：复习 6 小时、睡 8 小时，即 x = (6, 8)。

**第一步，算 h₁**——输入与权重逐位相乘再求和（即点积），加上偏置，过激活函数：

```text
0.5×6 + 0.2×8 + (−4) = 3 + 1.6 − 4 = 0.6
ReLU(0.6) = 0.6                          ← 正数原样通过
```

**第二步，算 h₂**，同样做法：

```text
0.1×6 + 0.5×8 + (−3) = 0.6 + 4 − 3 = 1.6
ReLU(1.6) = 1.6
```

**第三步，输出层**。注意它的输入不再是原始数据，而是上一层的两个结果 (0.6, 1.6)：

```text
1.5×0.6 + 0.8×1.6 + (−1) = 0.9 + 1.28 − 1 = 1.18
```

再过一道 Sigmoid 压进 0~1，当概率读：

$$\sigma(1.18) = \frac{1}{1 + e^{-1.18}} = 0.76$$

**结论：通过概率 76%。** 整个神经网络做的就是这七八次乘加，没有别的。

### 换几组输入看它的行为

```text
复习 6，睡 8   →  h₁=0.6   h₂=1.6   输出 1.18   →  76%
复习 2，睡 8   →  h₁=0     h₂=1.2   输出 −0.04  →  49%   ← h₁ 被 ReLU 归零
复习 10，睡 8  →  h₁=2.6   h₂=2.0   输出 4.50   →  99%
复习 6，睡 3   →  h₁=0     h₂=0     输出 −1.00  →  27%   ← 两个神经元全灭
```

复习越久概率越高、睡眠不足则大幅下降——这个函数确实"学会"了一点东西。

---

## 2. 术语对号入座

### 权重 = 重要性

某个输入对这个神经元有多重要，相当于斜率。h₁ 里复习的权重 0.5 大于睡眠的 0.2，说明它更看重复习。

```text
选房子考虑三个因素：
  面积 × 3（很重要）+ 卧室数 × 1（一般）+ 距离 × 0.5（不太重要）
  = 120×3 + 3×1 + 2×0.5 = 364

"加权" = 有的乘大数有的乘小数；"求和" = 加起来
```

### 偏置 = 门槛

h₁ 的偏置是 −4，意思是"加权和要先超过 4，我才开始输出正值"。看第二行：复习只有 2 小时时加权和是 2.6，不够 4，减完变负，ReLU 一刀砍成 0——**这个神经元没有被激活**。

"激活函数"这个名字正是由此而来：**它决定一个神经元在什么条件下才发声**。权重管"听谁的"，偏置管"多大声才理会"，两者缺一不可。

### 隐藏层 = 看不见的中间结果

h₁、h₂ 既不是你给的输入，也不是最终输出，从外部完全观察不到——只有输入和输出可见，中间层全藏在里面，所以叫**隐藏层**。

```text
输入层        隐藏层         隐藏层         输出层
(特征)       (自动提取)     (进一步提取)     (结果)

 x₁ ──→  ●  ──→  ●  ──→  ●
              ╲╱          ╲╱
 x₂ ──→  ●  ──→  ●  ──→  ● ──→ 输出
              ╱╲          ╱╲
 x₃ ──→  ●  ──→  ●  ──→  ●

每个 ● = 一个神经元（一次加权求和 + 偏置 + 激活）
每条线 = 一个权重参数
```

### 层 = 并排的一批神经元

h₁ 和 h₂ 吃同一份输入、互不依赖，因此可以同时算，它们同属一层。写成矩阵就是一次运算（见 [[矩阵与线性变换]]）：

$$h = \text{ReLU}\!\left(\begin{bmatrix}0.5 & 0.2\\ 0.1 & 0.5\end{bmatrix}\begin{bmatrix}6\\8\end{bmatrix} + \begin{bmatrix}-4\\-3\end{bmatrix}\right) = \text{ReLU}\begin{bmatrix}4.6 - 4\\4.6 - 3\end{bmatrix} = \begin{bmatrix}0.6\\1.6\end{bmatrix}$$

**权重矩阵的每一行 = 一个神经元的权重。** 一次矩阵乘法把整层算完——这就是 GPU 能大幅加速训练的原因。

---

## 3. 激活函数为什么非有不可

不是"加上效果更好"，是**没有它深度完全失效**。去掉激活函数把两层展开：

$$W_2(W_1 x + b_1) + b_2 = (W_2W_1)\,x + (W_2b_1 + b_2)$$

而 $W_2W_1$ 仍然是一个矩阵，$W_2b_1 + b_2$ 仍然是一个向量。也就是说结果仍是 $Wx + b$ 的形式——**无论叠多少层，都等价于单独一层**，参数白加、深度白费。

```text
无激活函数：
  第1层: y = 2x + 1
  第2层: z = 3y + 4 = 3(2x+1) + 4 = 6x + 7
  → 两层 = 一层，永远是直线

有激活函数：
  第1层: y = ReLU(2x + 1)
  第2层: z = ReLU(3y + 4)
  → 能表达折线、弯曲、复杂形状
```

ReLU 那个"负数归零"的动作破坏了线性，层数才开始有意义。

---

## 4. ReLU（最常用的激活函数）

读作 **"瑞露"**（/ˈrelʊ/），全称 Rectified Linear Unit（修正线性单元）。规则极其简单：**负数变 0，正数不变。**

```text
ReLU(-3) = 0     ReLU(0) = 0     ReLU(5) = 5
```

### 它怎么"变弯"

一个 ReLU 把直线砍一刀变成折线，多个叠加就能逼近任意曲线：

```text
神经元A：在 x=2 处砍一刀 → 一个折点
神经元B：在 x=5 处砍一刀 → 又一个折点
A + B 叠加 → 两个折点，斜率在 x=2 和 x=5 处变化

3 个神经元   → 粗糙的折线
100 个神经元 → 密到看起来像光滑曲线
```

**每多一个神经元就多砍一刀，砍得越多越能逼近任意形状。** 折点位置由偏置决定，斜率由权重决定——这正是第 2 节两个概念的几何意义。

### 常见的其他选择

| 激活函数 | 作用 | 常见位置 |
|---|---|---|
| ReLU | 负数归零，简单快、不易梯度消失 | 隐藏层默认选择 |
| Sigmoid | 压进 0~1 | 二分类的输出层（本文例子） |
| Softmax | 一组数压成和为 1 的概率分布 | 多分类输出层、注意力权重 |
| GELU / SiLU | ReLU 的平滑版 | 现代大模型的隐藏层 |

---

## 5. 整个网络就是一行公式

把三步合起来：

$$y = \sigma\big(W_2 \cdot \text{ReLU}(W_1 x + b_1) + b_2\big)$$

**神经网络的全部定义就是这一行。** 所谓"深"，只是这个套娃再多套几十上百层：

$$y = \sigma\big(W_n \cdot \text{ReLU}(\cdots \text{ReLU}(W_1 x + b_1)\cdots) + b_n\big)$$

```text
浅层神经网络: 输入 → 1~2 层 → 输出      ← 传统神经网络
深度神经网络: 输入 → 几十上百层 → 输出   ← 深度学习
```

层越多能学的模式越复杂，但也更吃数据和算力。注意**神经网络 ⊋ 深度学习**：浅层网络也是神经网络，只是不叫深度学习（见 [[Index]] 的边界说明）。

### 逐层提取特征

每层把上一层的简单发现组合成更复杂的发现，这是深度真正的价值：

```text
图像识别中：
  第1层：边缘、线条
  第2层：纹理、角、简单形状
  第3层：眼睛、耳朵、鼻子
  第4层：整张脸
```

---

## 6. 参数从哪来

第 1 节直接给了 9 个数，它们的构成是：

```text
隐藏层: 2 个神经元 × 2 个输入 = 4 个权重  +  2 个偏置
输出层: 1 个神经元 × 2 个输入 = 2 个权重  +  1 个偏置
────────────────────────────────────────────────
合计 9 个参数
```

**训练就是找这 9 个数的过程**：先随机初始化，拿一批已知答案的数据跑一遍前向计算，用损失函数量出预测与真实答案的差距，再沿"让损失变小"的方向微调每一个参数，反复几万次。反向传播是高效算出"每个参数该往哪调"的算法（详见 [[大模型如何训练]]）。

所以"7B 参数模型"的意思就是这样的数有 70 亿个，GPT-3 的 1750 亿同理。**结构上和上面这个 9 参数的小网络没有本质区别，只是数量级不同。**

---

## 7. 神经网络的变体

| 变体 | 连接方式 | 擅长 |
| --- | --- | --- |
| **全连接网络** | 每个神经元连接上一层所有神经元 | 表格数据 |
| **CNN** | 用固定大小的卷积核滑窗扫描 | 图像 |
| **RNN** | 带记忆，逐步处理序列 | 语音、文本 |
| **Transformer** | 用 Attention 全局连接 | 当前主流（GPT、BERT） |

它们都是神经网络，区别只在"哪些神经元连哪些"以及"权重怎么共享"，第 1 节那套加权求和 + 偏置 + 激活的机制完全不变。详见 [[序列模型]] 与 [[Transformer]]。

---

## 8. 关于"神经"这个名字

1943 年 McCulloch 与 Pitts 提出这个模型时，灵感确实来自生物神经元的"接收信号 → 超过阈值 → 放电"，第 2 节的偏置门槛就是这个阈值的残影。

但现代神经网络与真实大脑的相似度非常低：反向传播在生物学上没有对应机制，真实神经元的行为也远比加权求和复杂。**这个名字是历史遗留，它不提供任何理解上的帮助，反而容易让人以为里面藏着类脑的玄机。** 把它当作"一个参数很多的分段线性函数"来理解，更接近实情。

### 和之前知识的对应

| 之前学的概念 | 其实就是 |
| --- | --- |
| `y = w×x + b`（线性回归） | 一个没有激活函数的单个神经元 |
| 参数 w | 神经元之间连线上的权重 |
| 参数 b | 神经元的门槛（偏置） |
| 点积 | 一个神经元的加权求和 |
| 矩阵乘法 | 一整层神经元的加权求和 |
| "学习" | 调整所有权重和偏置，让损失函数最小 |

---

## 相关文章

- [[模型里的四类数字]] - 本文第 1 节那 9 个数属于哪一类，与特征、激活值怎么分
- [[向量与点积]] - 加权求和与点积的数学地基
- [[线性代数入门]] - 线性/仿射/非线性的区别，解释第 3 节为什么必须有激活函数
- [[矩阵与线性变换]] - 一层 = 一次矩阵乘法，本文第 2 节的矩阵形式
- [[机器学习基础概念]] - 损失函数、过拟合、深度学习 vs 传统方法
- [[监督学习任务]] - 回归、分类、搜索、推荐
- [[序列模型]] - RNN、CTC、Attention
- [[Transformer]] - 自注意力、QKV、BERT/GPT/T5 三大流派
- [[大模型如何训练]] - 这些参数具体怎么被找出来

---

## 参考资料

- 《动手学深度学习》第一章 - 引言
