#AI #index

# AI 知识地图

> Library/AI 总索引：学习顺序、知识树、每篇的定位与前置关系

## 推荐学习顺序（一条线走完）

```text
① 向量与点积          数学地基：一排数字怎么算"像不像"
      ↓
② 线性代数入门        数学地基：什么是线性函数、矩阵为什么长这样、符号怎么读
      ↓
③ 矩阵与线性变换      数学地基：一整层的计算怎么一次算完
      ↓
④ 机器学习基础概念     什么是"学习"：数据、损失函数、过拟合
      ↓
⑤ 监督学习任务        学习能干什么：回归、分类、搜索、推荐
      ↓
⑥ 神经网络           最小积木：神经元 = 加权求和 + 偏置 + 激活函数
      ↓
⑦ 序列模型           处理语音/文本：RNN → CTC → Attention
      ↓
⑧ 大语言模型          总览：输出概率分布、自监督预训练、三阶段
      ↓
⑨ 推理全流程          一次调用的机械过程：每步的形状与运算
      ↓
⑩ Transformer        Attention 做到极致：QKV、多头、三大流派
      ↓
⑪ Token 与 Embedding 表   文字怎么进模型：字节 → token → 向量
      ↓
⑫ 大模型如何训练      所有参数从哪来：猜下一个词 + 梯度下降
      ↓
⑬ RAG               工程应用：检索增强生成
```

原则：**⑩⑪⑫ 三篇互为犄角**（架构、输入、训练），顺序可以互换，但都依赖 ①~⑦；
⑧⑨ 是它们的总览——⑧ 讲"它是什么"，⑨ 讲"一次调用具体怎么走"，
先读这两篇，再看后三篇各自在补哪一格。
①②③ 是纯数学地基，急于上手可先跳到 ④，遇到公式看不懂时再回头补；
其中 ② 是零基础入口，看不懂数学符号（∑、下标、$f(u+v)$ 这类）就从它开始。

---

## 知识树（分类与包含关系）

**缩进 = 真包含关系**：机器学习 ⊋ 神经网络方法 ⊋ 深度学习 ⊋ 大语言模型，层层嵌套的子集，不是并列的几个门类。

```text
人工智能 AI
│
├── 数学地基（不属于 AI，但处处用到）
│   ├── 向量与点积 ················ 向量、加权求和、点积=相似度、维度固定
│   ├── 线性代数入门 ·············· 线性函数的定义与判定、仿射、∑ 等记号扫盲、
│   │                              线性函数由基向量决定 → 矩阵从哪来
│   ├── 矩阵与线性变换 ············ 矩阵乘法=多次点积、线性映射的复合、
│   │                              行/列两种读法、批量计算与 GPU
│   └── 1.05 字符编码（Python/）···· 字符→字节：ASCII、Unicode、UTF-8 模板填空
│
└── 机器学习（从数据中学规律，而非人工编写规则）
    │
    ├── 通用基础（不分传统/深度，都适用）
    │   ├── 机器学习基础概念 ······ 数据集/样本/特征、维数、i.i.d.、
    │   │                          损失函数（平方误差/交叉熵/极大似然推导）、
    │   │                          梯度下降与学习率、超参数、
    │   │                          过拟合、深度学习 vs 传统方法、三种学习方式
    │   └── 监督学习任务 ·········· 回归、分类、搜索、推荐
    │
    ├── 传统方法（非深度，人工设计特征）
    │   └── 决策树 / 随机森林 / SVM / KNN / GBDT（XGBoost、LightGBM）
    │       └── 暂无笔记；在表格类结构化数据上至今仍常胜过深度学习
    │
    └── 神经网络方法（神经元按层连接的一类模型）
        │
        ├── 神经网络 ·············· 神经元=加权求和+偏置+激活、ReLU、隐藏层、
        │                          完整数值演算、逐层提取特征、
        │                          变体一览（全连接/CNN/RNN/Transformer）
        │                          ↑ 基础概念，浅层网络也在此列
        │
        └── 深度学习（层数多起来之后：自动学特征、端到端训练）
            │
            ├── 序列模型 ·········· RNN（记忆）→ CTC（对齐）→ Attention（回看）
            │
            └── 大语言模型 LLM
                ├── 总览:  大语言模型 ···· 输出概率分布、自回归、参数构成、
                │                        自监督预训练、预训练→SFT→RLHF 三阶段、
                │                        base vs instruct、幻觉的由来
                ├── 流程:  推理全流程 ···· 分词→查表→位置编码→32 层→输出头→
                │                        采样→自回归；各步数据形状、KV 缓存、
                │                        注意力 O(n²)、常见现象对照
                ├── 架构:  Transformer ·· 自注意力 QKV、softmax、多头、位置编码、
                │                        前馈/残差/层归一化、编码器-解码器、
                │                        BERT/GPT/T5 三大流派
                ├── 输入:  Token 与 Embedding 表  分词器/词表/字节兜底、embedding 表、
                │          ↑ 前置: 1.05 字符编码    词级 vs 句级向量、LLaMA
                └── 训练:  大模型如何训练  题库自造、概率表、损失、反向传播、
                                          梯度下降、词向量相近的由来

工程应用（横跨各层：拿现成模型干活，不是上面任何一层的子集）
├── RAG ······················· 检索增强生成 = BERT 后代（检索）+ GPT 后代（生成）
├── Embedding Service（Database/） 文字进、向量出的句级服务
└── Vector Database（Database/）·· 向量的存储与相似度检索
```

> 跨文件夹的三篇（字符编码、Embedding Service、Vector Database）物理上不在 AI/ 里，
> 但属于这棵树，已双链打通。

**三条容易混淆的边界**：

其一，深度学习只是机器学习的一个分支，不能拿来当机器学习的同义词——「传统方法」那一支不是历史遗迹，在结构化数据上仍是首选。

其二，**深度学习 ≠ 神经网络**。神经网络是模型类（只要神经元按层连接就算，含单层感知机、单隐藏层的浅层网络）；深度学习特指层数多的情形，外加端到端训练、表示学习这套方法论，以及让深层网络训得动的配套技术（反向传播工程实现、ReLU、残差连接、批归一化、Dropout）。层数没有严格门槛，惯例上两层以上隐藏层算"深"。反向也不成立：深度森林 gcForest 是非神经网络的深度模型，说明"深度"指的是层次化表示这个思想，神经网络只是它最成功的载体。

其三，工程应用不参与上面的包含关系，它是消费方：RAG 用的是别人训好的模型，本身不涉及"学规律"。

---

## 各篇定位一览

| # | 笔记 | 一句话定位 | 直接前置 |
|---|------|-----------|---------|
| ① | [[向量与点积]] | AI 的通用语言：一排数字与相似度 | 无 |
| ② | [[线性代数入门]] | 什么是线性、矩阵从哪来、符号怎么读 | ① |
| ③ | [[矩阵与线性变换]] | 一次算完一整层的语言 | ①② |
| ④ | [[机器学习基础概念]] | "学习"的定义、打分与陷阱 | 无 |
| ⑤ | [[监督学习任务]] | 有标准答案时能做的事 | ④ |
| ⑥ | [[神经网络]] | 加权求和+偏置+激活叠成的函数 | ①②③④ |
| ⑦ | [[序列模型]] | 序列数据三件套 RNN/CTC/Attention | ⑥ |
| ⑧ | [[大语言模型]] | LLM 全貌：猜下一个词 + 三阶段训练 | ①④⑥ |
| ⑨ | [[推理全流程]] | 一次调用从输入到输出的每一步 | ①③⑥⑧ |
| ⑩ | [[Transformer]] | 纯 Attention 架构与三大流派 | ①③⑦⑨ |
| ⑪ | [[Token 与 Embedding 表]] | 文字→向量的两次查表 | ①、[[1.05 字符编码]] |
| ⑫ | [[大模型如何训练]] | 参数从随机数到智能 | ④⑩⑪ |
| ⑬ | [[RAG]] | 检索 + 生成的工程合体 | ⑨⑩⑪、[[Vector Database]] |

---

## 概念速查（这个词在哪篇里讲透了）

| 概念 | 所在笔记 |
|------|---------|
| 向量、向量空间、基、内积、加权求和、点积、余弦相似度、king−man+woman、维度 | [[向量与点积]] |
| 线性函数、可加性、齐次性、仿射、正齐次、线性组合、基向量、张成、∑ 求和符号、下标 | [[线性代数入门]] |
| 矩阵、矩阵乘法、尺寸规则、转置、单位矩阵、逆矩阵、线性变换、批量计算 | [[矩阵与线性变换]] |
| 损失函数/代价函数/目标函数、交叉熵、极大似然、梯度下降、学习率、超参数、过拟合、i.i.d. | [[机器学习基础概念]] |
| 神经元、权重、偏置、激活函数、ReLU、Sigmoid、隐藏层、层 | [[神经网络]] |
| 自回归、temperature、自监督、base model、指令微调 SFT、RLHF/DPO、幻觉、上下文窗口 | [[大语言模型]] |
| 数据形状、logits、采样策略(greedy/top-k/top-p)、因果掩码、KV 缓存、prefill/decode、O(n²) | [[推理全流程]] |
| RNN、隐状态、CTC、梯度消失 | [[序列模型]] |
| Q/K/V、自注意力、softmax、temperature、多头、位置编码、残差连接、层归一化、BERT/GPT/T5 | [[Transformer]] |
| token、分词器、词表、字节兜底、embedding 表、LLaMA | [[Token 与 Embedding 表]] |
| 猜下一个词、反向传播、梯度下降、参数(7B/70B) | [[大模型如何训练]] |
| Unicode、UTF-8、码点、模板填空、乱码 | [[1.05 字符编码]] |
| 向量检索、embedding 服务 | [[Embedding Service]]、[[Vector Database]] |

---

## 维护说明

- 新增 AI 笔记时：在知识树挂到对应分支、在学习顺序里插位、补一行概念速查
- 知识树的缩进表示真包含关系，挂分支时先确认从属，别挂成平级
- 数学地基三篇分工：[[向量与点积]] 讲对象，[[线性代数入门]] 讲概念与记号，[[矩阵与线性变换]] 讲运算
- 文件名未加序号前缀（区别于 Python/HTTP 系列），避免破坏既有双链；顺序以本索引为准
- 已留的坑：
  - 传统机器学习方法（决策树、SVM、GBDT）→ 挂在「传统方法」下，目前是空分支
  - 指令微调与 RLHF 已在 [[大语言模型]] 第 6 节写了，若要展开可另起一篇
